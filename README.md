## Instalación

Este proyecto utiliza Conda para la gestión del entorno.

1. Clona el repositorio.
2. Crea el entorno desde el archivo yml:
   ```bash
   conda env create -f environment.yml
3. Activa el entorno:
```bash
    conda activate taylor_env
4. Ejecuta la aplicación:
    streamlit run app.py